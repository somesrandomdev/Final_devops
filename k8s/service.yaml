apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: demo
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
  - port: 80
    targetPort: 8000
    nodePort: 30080   # Optional: fix to a specific port (30000-32767), or remove for auto-assign